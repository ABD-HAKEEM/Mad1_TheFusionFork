plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.the_fution_fork_mad"
    compileSdk = 34 // Updated to SDK version 34

    ndkVersion = "25.2.9519653" // Ensure this matches the installed NDK version

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
        applicationId = "com.example.the_fution_fork_mad"
        minSdk = 21 // Minimum SDK version
        targetSdk = 34 // Updated to SDK version 34

        versionCode = 1
        versionName = "1.0.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug // Update with a release key for production
        }
    }
}

flutter {
    source = "../.." // Ensure this points to the correct Flutter module directory
}
